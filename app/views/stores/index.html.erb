<div class="container">
  <ul>
    <% @stores.each do |store| %>
      <li>
        <%= link_to store.name, store_path(store) %> | City: <%= store.address %>
        <br>
        <%= store.description %>
      </li>
      <br>
    <% end %>
  </ul>
</div>
